[project]
name = "new-folder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "amrish-chavan", email = "amrishchavan537@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "dhanhq>=2.2.0",
    "dotenv>=0.9.9",
    "kiteconnect>=5.2.2",
    "load-dotenv>=0.1.0",
    "pyotp>=2.10.0",
]

[project.scripts]
new-folder = "new_folder:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
